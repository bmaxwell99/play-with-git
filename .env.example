# Copy to .env (which is gitignored) and fill in. Never commit real keys.

# seats.aero Partner API key — https://seats.aero (Pro/API tier, ~$10/mo)
SEATSAERO_API_KEY=

# Anthropic API key — only needed for --llm ranking
ANTHROPIC_API_KEY=
